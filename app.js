import React from "react";
import  ReactDOM  from "react-dom";

const heading = (<h1 className="heading">I'm a react element</h1>);

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);

